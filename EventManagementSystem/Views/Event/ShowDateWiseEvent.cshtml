
@{
    ViewBag.Title = "ShowDateWiseEvent";
    Layout = "~/Views/Shared/Event.cshtml";
}

<h2>Show Date Wise Event</h2>
<form id="eventForm">
    <label>Start Time</label>
    <input type="date" id="startDate" />
    <label>End Time</label>
    <input type="date" id="endDate" />
    <input type="button" id="Show" value="Show" class="btn btn-default" style="background-color: #FF0000; color: white" />
</form>
<br />


<form>
    <table class="table table-striped table-bordered" id="tableId">
        <thead>
            <tr>
                <th>Event Name</th>
                <th>Venue </th>
                <th>Date</th>
                <th>Start Time</th>
                <th>End Time</th>

            </tr>
        </thead>
        <tbody id="StudentTableBody"></tbody>


    </table>
</form>
<h3 style="text-align: center"><label id="displayMsg"></label> </h3>


@section scripts
{

    <script src="~/Scripts/jquery-1.10.2.min.js"></script>
<script src="~/Scripts/jquery.validate.min.js"></script>
    <script>
        $(document).ready(function () {
            $('#eventForm').validate({
                rules: {
                    startDate: "Required",
                    endDate: "Required",
        }
            });
            $('#Show').click(function() {
                var fromDate = $('#startDate').val();
                var toDate = $('#endDate').val();
                var startdate = Date.parse(fromDate);
                var enddate = Date.parse(toDate);
        
                if (startdate > enddate) {
                    $("#displayMsg").text("Start Date must be less than end time");
                }
                else {

                    var jsonData = { frmDate:fromDate, edDate: toDate };
                $.ajax({
                    type: "POST",
                    url: '@Url.Action("ShowDateWiseEventByRange", "Event")',

                    contentType: "application/json; charset=utf-8",
                    data: JSON.stringify(jsonData),
                    dataType: "json",
                    success: function (data) {
                        $.each(data, function (key, value) {
                            $("#StudentTableBody").append('<tr><td>' + value.EventName + '</td><td>' + value.EventVenue + '</td><td>' + value.EventDate.converters + '</td><td>' + value.StartTime + '</td><td>' + value.EndTime + '</td></tr>');
                        });
                    }


                });

            }

            });

        });
    </script>
}

